<%= render "sidebar" %>

<div id="main">

<%= form_for(@loan_item) do |form| %>
  <%= render "form", f: form %>
  <%= form.submit "登録" %> </br>
  <%= debug(params) if Rails.env.development? %>
<% end %>

</duv>
