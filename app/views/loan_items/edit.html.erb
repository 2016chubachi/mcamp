<%= render "sidebar" %>

<div id="main">

<%= form_for(@loan_item) do |form| %>
  <%= render "form", f: form %>
  <%= form.submit "変更" %> </br>
  <%= debug(params) if Rails.env.development? %>
<% end %>

</div>
