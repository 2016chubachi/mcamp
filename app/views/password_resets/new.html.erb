<div id="login-main">
  <% provide(:title, "Forgot password") %>

<!--notice-->
  <% if flash.notice %>
      <div class="tn-box tn-box-color">
        <p id="p-tn-box"><%= flash.notice %></p></div>
  <% end%>
<!--warnning-->
  <% if flash.alert %>
      <br/><div class="tn-box tn-box-color">
  <p id="p-tn-box"><%= flash.alert %></p></div>
  <% end%>


  <div>

    <h2 class="heading">Reset Password</h2>
    <%= form_for(:password_reset, url: password_resets_path) do |f| %>
        <div class="controls">
          <%= f.email_field :email, class: "floatLabel" %>
          <%= f.label :email, "EMAIL" %>
        </div>

        <%= f.submit "Comfirm", class: "controls button" %>
    <% end %>
  </div>
</div>
<br/>
<br/>

<!--script src="http://libs.useso.com/js/jquery/2.1.1/jquery.min.js" type="text/javascript"></script-->
<script>window.jQuery || document.write('<script src="/assets/jquery-2.1.1.min.js"><\/script>')</script>
<script>
  (function ($) {
    function floatLabel(inputType) {
      $(inputType).each(function () {
        var $this = $(this);
        $this.focus(function () {
          $this.next().addClass('active');
        });
        $this.blur(function () {
          if ($this.val() === '' || $this.val() === 'blank') {
            $this.next().removeClass();
          }
        });
      });
    }
    floatLabel('.floatLabel');
  }(jQuery));
</script>


